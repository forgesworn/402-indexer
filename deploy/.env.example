# Required: 64-character hex Nostr secret key for the indexer pubkey
INDEXER_SECRET_KEY=

# Optional: GitHub personal access token for higher API rate limits
GITHUB_TOKEN=

# Optional: path to health state file (defaults to health-state.json in cwd)
HEALTH_STATE_PATH=/opt/402-indexer/health-state.json
